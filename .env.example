# Supabase (Project Settings -> API)
SUPABASE_URL=
# Project Settings -> API -> JWT Secret
SUPABASE_JWT_SECRET=
# Project Settings -> API -> service_role key (secret — bypasses RLS, backend-only)
SUPABASE_SERVICE_ROLE_KEY=

